# Add code here

version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: express-app
    ports:
      - "4567:4567"
    environment:
      - PORT=4567
    restart: unless-stopped